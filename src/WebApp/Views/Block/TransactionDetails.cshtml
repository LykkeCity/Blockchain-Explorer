@model BitcoinChainExplorerForAspNet5.Models.TransactionModel
@{
    Layout = null;
}

<div class="icon">
    <span class="currency-icon thumbnail">
        <span class="bg-warning">
            <i class="fa fa-btc"></i>
        </span>
    </span>
</div>
<div class="inputs">
    @foreach (var input in Model.Inputs)
    {
        <div class="address-item ">
            <div class="amount">
                - @($"{Convert.ToDecimal(input.Value):G29}")
            </div>
            <div class="address">
                <i class="fa fa-lg fa-arrow-left"></i>
                @input.Addresses
            </div>
        </div>
    }
</div>

<div class="outputs">
    <div class="outputs-container">
        <div class="center-chevron">
            <i class="fa fa-chevron-right fa-lg"></i>
        </div>

        @foreach (var output in Model.Outputs)
        {
            <div class="address-item ">
                <div class="amount">
                    @($"{Convert.ToDecimal(output.Value):G29}")
                </div>
                <div class="address">
                    @output.Addresses
                </div>
            </div>
        }
        <div class="address-item ">
            <div class="amount">
                @($"{Convert.ToDecimal(Model.Fee):F8}")
            </div>
            <div class="address">
                Fees
            </div>
        </div>
</div>
</div>
